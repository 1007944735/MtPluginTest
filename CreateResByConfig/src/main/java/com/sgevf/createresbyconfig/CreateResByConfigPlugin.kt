package com.sgevf.createresbyconfig

import org.gradle.api.Plugin
import org.gradle.api.Project

class CreateResByConfigPlugin : Plugin<Project> {
    override fun apply(target: Project) {
        println("Hello")
    }
}